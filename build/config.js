var src = 'src';
var dist = 'dist';

var config = {
	src: src,
	dist: dist,

	styles: {
		src: [
			src + '/base.css',
			src + '/textfield.css',
			src + '/select.css',
			src + '/box.css',
			src + '/button.css',
			src + '/hint.css',
			src + '/tooltip.css',
			src + '/preloader.css',
			src + '/grid.css'
		],

		outputName: 'ceres'
	}
};

module.exports = config;
